
  {{#if event }}
    {{#each event }}

  <section class="col-md-12">
    <div class="row">
        <div class="col-md-9">
          <div class="home-card mdl-card mdl-shadow--2dp">
            <div class="row">
              <div class="col-md-12">
                <h1>  {{ name }} </h1>
                  <div class="mdl-card__supporting-text">
                    Start: {{ dataStart }} | {{description}}
                  </div>
              </div>

              <div class="col-md-6">
                    {{#if status }}
                    <a href="../event/{{actionBtn}}/{{id}}" class="header-button">
                    <button class="btn btn-default">{{status}}</button>
                    </a>
                    {{/if}}
              </div>
            </div>

            </div>
        </div>
    </div>
    </section>

    {{/each}}

  {{/if}}
